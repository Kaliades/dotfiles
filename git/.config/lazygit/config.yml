gui:
  language: "en"
git:
  diffRenderers:
    - colorArg: always
      command: delta --paging=never --no-gitconfig --syntax-theme="${BAT_THEME:-Catppuccin Mocha}" --navigate --hunk-header-style=omit --hyperlinks --true-color=always
